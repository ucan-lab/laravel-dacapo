example1:
  columns:
    id: bigIncrements
    name: string
    email: string
  indexes:
    - columns: name
      type: index

example2:
  columns:
    id: bigIncrements
    name: string
    email: string
  indexes:
    - columns: [name, email]
      type: index

example3:
  columns:
    id: bigIncrements
    name: string
    email: string
  indexes:
    - columns: [name, email]
      type: index
      name: users_name_index
