users:
  columns:
    id: bigIncrements
    name: string
    email:
      type: string
      unique:
    email_verified_at:
      type: timestamp
      nullable:
    password: string
    rememberToken:
    timestamps:

password_resets:
  columns:
    email:
      type: string
      index:
    token: string
    created_at:
      type: timestamp
      nullable:

failed_jobs:
  columns:
    id:
    uuid:
      type: string
      unique:
    connection: text
    queue: text
    payload: longText
    exception: longText
    failed_at:
      type: timestamp
      useCurrent:

personal_access_tokens:
  columns:
    id:
    tokenable: morphs
    name: string
    token:
      type: string
      args: 64
      unique:
    abilities:
      type: text
      nullable:
    last_used_at:
      type: timestamp
      nullable
    timestamps:
